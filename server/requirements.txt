anndata==0.8.0
bitarray==2.8.1
boto3==1.28.17
click==8.1.7
envyaml==1.10.211231
flask==2.3.3
flask-compress==1.14
flask-cors==4.0.0
flask-restful==0.3.10
flask-talisman==1.1.0
flatbuffers>=1.11.0,<2.0.0 # cellxgene is not compatible with 2.0.0. Requires migration
flatten_dict==0.4.2
fsspec==2023.9.1
gunicorn[gevent]==21.2.0
numba==0.57.1
numpy==1.23.5
pandas==2.0.3 #2.1.0 is the latest version, but it is not available in GHA testrunner
pydantic==1.10.12 # 2.3.0 is the latest version but the major upgrade to 2 breaks csp directives and associated tests
requests==2.31.0
scipy==1.10.1 #1.11.2 is the latest version, but it is not available in GHA testrunner
flask-server-timing==0.1.2
s3fs==2023.9.1
tiledb==0.23.0 # Explorer's major/minor tiledb version should always be the >= Portal's tiledb major/minor version (for read/write compatibility)
Werkzeug==2.3.7 # 2.3.0 breaks the binary endpoints
python-json-logger==2.0.7
